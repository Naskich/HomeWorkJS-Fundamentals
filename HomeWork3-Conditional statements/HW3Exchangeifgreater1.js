//Problem 1. Exchange if greater
//Write an if statement that takes two double variables a and b and 
//exchanges their values if the first one is greater than the second.
//As a result print the values a and b, separated by a space.
var a = 3, b = 4;
var sumA= a + " " ;
var sumB= b + " " ;
var resulta = (sumA + sumB);
var resultb = (sumB + sumA);
if (a>b){
console.log("result is  ", resultb);
} else { 
console.log("result is ", resulta);
}